<!DOCTYPE html>
<html lang="en" data-theme="pastel" class="bg-gray-200">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/main.css">
  <title>Sets - Lego Collection</title> <!-- Updated title -->
</head>
<body>
    <%- include('partials/navbar', { page: '/lego/sets' }) %>
    
    <div class="container mx-auto">
      <div class="hero bg-base-200 rounded-lg p-6">
        <div class="hero-content text-center ">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold">Collection</h1>
            <p class="py-6">Browse the entire collection. Click the theme to filter by themes such as: </p>
              <a href="/lego/sets?theme=Technic" class="btn btn-primary btn-xs">Technic</a>
              <a href="/lego/sets?theme=Classic Town" class="btn btn-primary btn-xs">Classic Town</a>
              <a href="/lego/sets?theme=supplemental" class="btn btn-primary btn-xs">Supplemental</a>
            </p>
          </div>
        </div>
      </div>
    <br>
      <div class="overflow-x-auto overflow-y-hidden">
        <table class="table table-zebra">
          <tbody>
            <!--- head -->
            <% sets.forEach(function(set) { %>
                    <tr>
                        <td>
                          <div class="avatar">
                            <div class="w-24 rounded-xl">
                              <img src="<%= set.img_url %>" alt="<%= set.name %>" class="w-32 h-32 rounded-lg bg-transparent bg-no-repeat bg-cover">
                            </div>
                          </div>
                        </td>
                        <td>
                          <span class="font-bold"><%= set.name %> | 
                        </td>
                        <td>
                            <a href="/lego/sets?theme=<%= set.Theme.name %>" class="btn mt-2 btn-xs">
                                <%= set.Theme.name %>
                            </a>
                        </td>
                        <td>
                          <span class="font-bold">Year: <%= set.year %>
                          <span class="font-bold">- Parts: <%= set.num_parts %>
                        </td>
                        <th>
                          <a href="/lego/sets/<%= set.set_num %>" class="btn btn-accent btn-sm">Details</a>
                        </th>
                        </td>
                    </tr>
            <% }); %>
          </tbody>   
        </table>
      </div>
    </div>
</body>
    <div style="all: initial !important;"></div>
</html>
